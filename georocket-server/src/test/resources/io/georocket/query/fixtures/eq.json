{
  "query": "EQ(foo bar)",
  "queryCompilers": ["io.georocket.index.generic.GenericAttributeIndexerFactory"],
  "expected": {
    "$or": [{
      "genAttrs": {
        "$elemMatch": {
          "key": "foo",
          "value": "bar"
        }
      }
    }, {
      "props.foo": "bar"
    }]
  }
}
