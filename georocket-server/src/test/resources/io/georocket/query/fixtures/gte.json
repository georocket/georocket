{
  "query": "GTE(foo 12)",
  "queryCompilers": ["io.georocket.index.xml.XMLGenericAttributeIndexerFactory"],
  "expected": {
    "$or": [{
      "genAttrs.foo": {
        "$gte": "12"
      }
    }, {
      "props.foo": {
        "$gte": "12"
      }
    }]
  }
}
