{
  "query": "bla",
  "path": "/mypath",
  "expected": {
    "$and": [{
      "$or": [{
        "tags": "bla"
      }, {
        "props.value": "bla"
      }]
    }, {
      "path": {
        "$regex": "^/mypath"
      }
    }]
  },
  "expectedPg": {
    "where": "((data->'tags' ? $1 OR data->'props' @> $2) AND (data->'path')::text LIKE $3)",
    "params": ["bla", [{"value": "bla"}], "/mypath%"]
  }
}
