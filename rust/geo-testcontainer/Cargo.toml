[package]
name = "geo-testcontainer"
version = "0.1.0"
edition = "2021"

[dependencies]
testcontainers = "0.15.0"

[dev-dependencies]
postgres = "0.19.7"

[features]
default = []
postgis = []
postgis-experimental-arm64 = ["postgis"]
