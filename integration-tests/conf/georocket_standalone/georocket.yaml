georocket:
  host: 0.0.0.0
  storage:
    driver: file
    file:
      path: $GEOROCKET_HOME/storage
  index:
    mongodb:
      embedded: true
  embeddedMongoDB:
    storagePath: $GEOROCKET_HOME/storage/mongodb
