georocket:
  host: 0.0.0.0
  storage:
    driver: postgresql
  index:
    driver: postgresql
    postgresql:
      url: jdbc:postgresql://postgresql:5432/georocket
      username: postgres
      password: mysecretpassword
